node_modules
.next
out
.env
.env.local
.env.*.local
*.log
npm-debug.log*
.DS_Store
.idea
.vscode
next-env.d.ts

# TypeScript-Build-Artefakt
tsconfig.tsbuildinfo

# WordPress-Export der alten Live-Seite: reines Arbeitsmaterial für den
# Nachbau und nicht Teil der Anwendung. Enthält ausserdem Produktions-
# Zugangsdaten (wp-config.php) und den DB-Dump mit Benutzer-Hashes.
vorlage-wordpress/

# Sicherungsarchive und Git-Bundles. Liegen bewusst im Projektordner, gehören
# aber nicht ins Repository: Sie sind je rund 170 MB und überschreiten damit
# GitHubs Dateigrenze von 100 MB — ein Push scheitert daran. Ein früherer
# `git add -A` hatte sie versehentlich aufgenommen, die Historie musste
# deshalb bereinigt werden.
*.tar.gz
*.bundle
out/
hostpoint/mailer/konfig.php
.export-beiseite/
.vercel
.env*

# Wegwerf-Skripte und Zwischendateien der Bildwerkzeuge. Alles mit Punkt am
# Anfang unter werkzeuge/ ist einmalig benutzt oder abgeleitet und lässt sich
# jederzeit neu erzeugen — die dauerhaften Werkzeuge haben keinen Punkt.
werkzeuge/.*
